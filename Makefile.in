
xdms:	
	$(MAKE) -C src

clean:	
	$(MAKE) -C src clean

install:	xdms
	mkdir -p {PREFIX}/bin
	mkdir -p {PREFIX}/share/doc/xdms-{VERSION}
	install src/xdms {PREFIX}/bin/
	install xdms.txt COPYING ChangeLog.txt {PREFIX}/share/doc/xdms-{VERSION}/
